<% include header %>

<h4>Welcome, <%= teacher.name %></h4>

<br><br>

<div class="container">
    <div class="row">
        <div class="col s12 l12">
            <h4>Courses:</h4>
            <ul class="collection">
                <% teacher.courses.forEach(item => { %>
                    <li><%= item.name %></li>
                <% }); %>
            </ul>
        </div>
    </div>
</div>